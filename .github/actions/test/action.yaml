name: 'Register Components'
description: 'An action to register mfe components'
inputs:
  value:
    description: "JSON Value"
    required: true
outputs:
  secretValue:
    description: 'returns true if component is registered and false if registration fails'
runs:
  using: 'node16'
  main: 'src/index.js'
